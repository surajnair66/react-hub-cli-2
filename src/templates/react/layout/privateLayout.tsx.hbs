import { AppSidebar } from '@/components/app-sidebar';
import Header from '@/components/header';
import { SidebarInset, SidebarProvider } from '@/components/ui/sidebar';
import { ReactNode } from 'react';

interface Layout {
  children?: ReactNode;
}

const PrivateLayout: React.FC<Layout> = ({ children }) => {
  return (
    <SidebarProvider className='flex flex-col w-screen'>
      <Header />
      <div className='flex flex-1'>
        <AppSidebar />
        <SidebarInset className='flex-1 w-full flex flex-row bg-[#F0F4F8] overflow-hidden '>
          <div className='mt-[72px] w-full overflow-auto h-[calc(100vh-72px)]'>{children}</div>
        </SidebarInset>
      </div>
    </SidebarProvider>
  );
};

export default PrivateLayout;
